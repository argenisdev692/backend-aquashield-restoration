// ============================================================
//  ContactSupport — public contact-form submissions
//  - updatedAt managed by `set_updated_at_contact_supports` trigger.
// ============================================================

model ContactSupport {
  id         String    @id @default(dbgenerated("uuid_generate_v7()")) @db.Uuid
  firstName  String    @map("first_name") @db.VarChar(255)
  lastName   String    @map("last_name") @db.VarChar(255)
  email      String    @db.VarChar(255)
  phone      String    @db.VarChar(20)
  message    String
  smsConsent Boolean   @default(false) @map("sms_consent")
  readed     Boolean   @default(false)
  createdAt  DateTime  @default(now()) @map("created_at") @db.Timestamp(6)
  updatedAt  DateTime  @default(now()) @map("updated_at") @db.Timestamp(6)
  deletedAt  DateTime? @map("deleted_at") @db.Timestamp(6)

  @@index([readed], map: "idx_contact_supports_readed")
  @@map("contact_supports")
}
